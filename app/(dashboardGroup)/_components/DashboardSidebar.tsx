import { IUser } from '@/lib/types'
import React from 'react'

const DashboardSidebar = ({ user }: { user: IUser | null }) => {
  return (
    <div>DashboardSidebar</div>
  )
}

export default DashboardSidebar